export type User = {
  id: string;
  userName: string;
  namePreview: string;
  subscribersCount: number;
  isSubscribed: boolean;
};

export type Subscribe = {
  id: string;
  userName: string;
  namePreview: string;
};
